# LoveLive! HLTV 未来开发计划

> 基于 2026-08 项目现状盘点制定。Phase 1-3 已完成（见 README），本计划覆盖 Phase 4-8。
>
> 方针：数据先行 → 功能跟进 → 风格深化 → 工程化收尾 → 轻量后端（可选）。

## 现状盘点（2026-08）

| 模块 | 状态 | 短板 |
|------|------|------|
| 页面（11 个） | ✅ 完成 | 无代码分割，首屏加载全部页面 |
| 声优数据 `players.js` | ✅ 较完整（1293 行） | 无"加入/毕业"时间属性 |
| 组合数据 `groups.js` | ✅ 基本可用 | 缺官方小队（见下） |
| 歌曲数据 `songs.js` | ✅ 完成（239 首 / 全企划单曲+solo） | 已扩充字段（releaseDate/singleId/center/performers/词曲/type）；新增 `discs.js`（112 张唱片）；莲之空 104/105 期角色待 Phase 4.4 补录 |
| Live 数据 `lives.js` | ✅ 已增强 | setlist 已关联歌曲 ID，含出演名单/缺席明细/跨企划大合演；各企划非编号 Tour 场次可再扩充 |
| 关系数据 `relationships.js` | ⚠️ 单薄（37 行） | μ's/Aqours 用"年级组"占位，官方小队（Printemps/BiBi/lily white、CYaRon!/AZALEA/Guilty Kiss）未录入；无跨企划关系 |
| i18n | ✅ 完整 | — |
| 测试 | ❌ 零测试 | rating 算法、getDisplayName、i18n 映射等纯函数无保护 |
| CI/部署 | ❌ 无 | 无 GitHub Actions，未部署上线 |

---

## Phase 4 · 数据深化（优先级最高，新功能的前置依赖）

### 4.1 歌曲数据补全
- [x] 扩充字段：`releaseDate`（发行日期）、`singleId`（所属单曲）、`center`（Center 成员）、`performers`（演唱阵容）、`lyricist`/`composer`（作词/作曲）、`type`（single/album/solo/coupling）
- [x] 补录各企划完整单曲主打曲（μ's 全单曲、Aqours 全单曲、虹咲全单曲、Liella! 全单曲、莲之空全单曲）
- [x] 新建 `src/data/discs.js`：单曲/专辑数据（编号、名称、封面色、发行日期、销量、收录曲）
- [x] 莲之空、虹咲补充角色 solo 曲（Link! Like! 相关）
  - 附注：含动画插入歌 solo 单曲、专辑 solo 代表作、R3BIRTH 单曲、莲之空毕业专辑新录 solo 及 Link! Like! 翻唱 solo；μ's/Aqours 编号单曲已按官方定义修正（μ's 共 8 张编号单曲、Aqours 共 4 张）

### 4.2 官方小队数据修正
- [x] μ's：录入 Printemps / BiBi / lily white（替换或并列现有"年级组"）
- [x] Aqours：录入 CYaRon! / AZALEA / Guilty Kiss
- [x] 关系类型补充 `officialUnit`，与玩家自创 `cp`、年级 `subunit` 区分
  - 附注：虹咲（DiverDiva/A・ZU・NA/QU4RTZ/R3BIRTH）、Liella!（CatChu!/KALEIDOSCORE/5yncri5e!）、莲之空（スリーズブーケ/DOLLCHESTRA/みらくらぱーく！）官方小队一并归入 `officialUnit`；年级组保留 `subunit` 与官方小队并列展示
- [x] TeamDetail 关系图支持按类型筛选

### 4.3 Live 数据增强
- [x] setlist 关联歌曲 ID（当前为纯文本歌名）
- [x] 补充出演成员明细（哪些成员缺席/休止，如玲娜休止期记录）
- [x] 补录重要 Live：LoveLive! Fest、9th/10th Anniversary、甲子园、东京巨蛋场次
  - 附注：setlist 改为 `{ songId }` / `{ title, titleEn }` 对象格式（`resolveSetlistEntry` 兼容旧纯文本）；新增 `performers`（显式出演名单，修正虹咲 1st=9 人、Liella! 1st/2nd=5 人等期次阵容）与 `memberStatus`（缺席/部分出演明细：Fest 鬼頭明里仅 Day1、甲子园高槻かなこ辞演・鬼頭明里两日缺席・相良茉優/小泉萌香单日出演、虹咲 4th せつ菜役交棒林鼓子）；补录 LoveLive! Fest 2020（系列 9 周年）、Aqours Finale 永久stage（结成 10 周年 Finale）、ユニット甲子園 2024、Aqours EXTRA DREAM LAND 2022（第二次东蛋）；勘误 Aqours 5th 场馆（MetLife Dome）、μ's Final/Aqours 4th 等多日公演补充 endDate 与备注；MatchDetail 页新增公演备注横幅与出席明细（缺席红/部分出演黄徽章）区块

### 4.4 关系数据扩充
- [x] 跨企划"憧憬"关系（如 Aqours → μ's 的粉丝设定）
- [x] 声优层面的跨企划关系（同事务所、共演作品）
- [x] 莲之空 103 期 → 104 期成员世代标记
  - 附注：新增关系类型 `admiration`（憧憬：Aqours→μ's、ダイヤ→穂乃果）、`crossUnit`（跨企划组合：AiScReam，出自广播 ANN GOLD，2026.4 起セラス加入新体制）、`sameAgency`（同事务所：三森すずこ×進藤あまね同属響）；跨企划关系用 `groupIds` 数组归属，在所有涉及企划页展示，TeamDetail 关系卡支持 from→to 方向性展示与描述文字；蓮ノ空补录 104 期（吟子/小鈴/姫芽）与 105 期（セラス/泉）成员并添加 `generation` 字段（PlayerDetail 世代展示），官方小队更新为含新成员的历史全阵容（新录入 Edel Note），103/104/105 期世代组以 `subunit` 展示；PlayerDetail 对未公开字段（部分新声优生日/血型等）做隐藏容错
  - 附注（数据勘误）：修复 players.js 中 μ's/Aqours 段共 11 处声优映射错位（真实对应：凛=飯田里穂、真姫=Pile、花陽=久保ユリカ、にこ=徳井青空、希=楠田亜衣奈；ダイヤ=小宮有紗、曜=斉藤朱夏、善子=小林愛香、花丸=高槻かなこ、鞠莉=鈴木愛奈、ルビィ=降幡愛，误挂的斉藤千和/田中美海/大西亜玖璃已移除），并按官方资料修正生日/出身地/血型/身高/事务所（如新田恵海 12/10 長野、伊波杏樹 2/7 埼玉、鈴木愛奈 147cm、三森すずこ 山梨出身等）；无可靠来源的字段（Pile/高槻的生日年份、部分身高事务所）按数据原则省略；lives.js 的 AQOURS_PLAYERS 阵容 id 与甲子园 2024 缺席记录（花丸=高槻かなこ辞演）同步更新

---

## Phase 5 · 新功能页面

### 5.1 歌曲列表页 `/songs` + 详情页 `/songs/:id`
- [x] 列表：企划筛选、类型筛选、按发行日期/播放量/销量排序
- [x] 详情：基本信息、演唱阵容（链接到选手页）、Center、所属单曲、首次披露 Live、成绩数据
- [x] 依赖 Phase 4.1
  - 附注：新增 `Songs.jsx`（企划标签筛选 + 类型下拉筛选 + 排序键/升降序切换；缺 type 的歌曲兜底为"其他"）与 `SongDetail.jsx`（基本信息/Center/所属唱片色块与编号/首次披露 Live 链接 + 累计 Live 演出次数/播放量与销量及全站排名/演唱阵容芯片链接选手页并带 C 徽章/同唱片收录曲互链）；`seiyuu.js` 新增 `getLivesBySong` / `getSongFirstLive` 辅助函数；Header 导航新增 Songs 入口，全局搜索歌曲结果接入 `/songs/:id` 链接；i18n 补充 songs/songDetail 中英文案；构建验证通过（`npm run build`）

### 5.2 单曲/专辑页 `/discs`（可与 songs 合并设计）
- [x] 单曲时间线视图（HLTV events 列表风格）
- [x] 单曲详情：收录曲、封面色块、Oricon 峰值排名、销量
  - 附注：新增 `Discs.jsx`（按发行年份分组的时间线表格，仿 HLTV Events 年度列表；企划标签 + 唱片类型筛选 + 新旧排序切换；行内含封面渐变色块、编号、收录曲数、Oricon 峰值色编码 Top3 金/Top10 绿/Top30 黄、销量）与 `DiscDetail.jsx`（封面色块 + 光盘中心孔视觉、榜单成绩卡 Oricon 周榜峰值 + 销量及全站排名、基本信息含品番、收录曲 M1/M2... 编号列表带 Center C 徽章与类型徽章并互链歌曲页、部分入库曲目显示已入库 n/m 提示、同企划前后发行导航）；`discs.js` 末尾按销量分层自动估算 `oriconPeak` 字段（近似值，可在条目上手写真实值覆盖）；Header 导航新增 Discs 入口，全局搜索新增唱片分类（匹配唱片名/罗马音/编号），SongDetail"所属单曲"改为链接唱片详情页；i18n 补充 discs/discDetail 中英文案；构建验证通过（`npm run build`）

### 5.3 大型活动页 `/events`（对应 HLTV Events）
- [x] LoveLive! Fest、Animelo Summer Live、各企划 Anniversary 等大型活动
- [x] 活动详情：参演组合、场次、场地、关联 Live 链接
  - 附注：新增 `src/data/events.js`（14 场大型活动，分四类：festival 系列合同祭典 / anniversary 周年纪念 / external 外部音乐节 / award 电视舞台）；收录 LoveLive! Fest 2020、COUNTDOWN LoveLive! 2021→2022、ユニット甲子園 2024、μ's 3rd Anniversary、Aqours 结成 10 周年 Finale，以及 Animelo Summer Live 2012-2015（μ's）/2017-2019（Aqours・虹咲）/2023（Liella!・虹咲）共 8 届出演、第 66 回 NHK 红白歌合战（μ's 红白初出场）；活动数据经 anisama.tv 历年 setlist 与官方特设页交叉验证；lives.js 补录 COUNTDOWN 2021 合同 Live（三团 26 人编制 + 高槻かなこ出演确认备注）；新增 `Events.jsx`（仿 HLTV Events 年度分组时间线：企划标签 + 类型筛选 + 新旧排序，参演企划色点、类型徽章、场地、关联 Live 直达链接）与 `EventDetail.jsx`（基本信息 + 参演组合芯片 + 场次明细表含部分出演备注 + 关联 Live 卡片 + 时间线前后活动导航）；Header 导航新增 Events 入口；i18n 补充 events/eventDetail 中英文案；构建验证通过（`npm run build`）

### 5.4 选手生涯时间线（嵌入 PlayerDetail）
- [x] 声优视角：出道、加入企划、solo 专辑、重大活动节点
- [x] 角色视角：动画登场、中心曲、毕业节点
  - 附注：新增 `CareerTimeline.jsx` 组件（双视角 Tab + 新旧排序切换 + 年份分组纵向时间线，节点带类型徽章/企划色与角色色圆点，可内链歌曲/Live/活动详情页）；`seiyuu.js` 新增 `getCareerTimeline` 推导函数——声优视角（出道年份/以角色名义首次参与 CD 的加入企划日/Solo 曲发行/重大活动/初次 Live 出演）与角色视角（角色 CD 初披露/Center 担当曲）全部从 songs/lives/events 数据动态推导；动画登场与毕业节点新建 `src/data/timeline.js` 静态维护（各企划动画/App 登场日期 + 追加成员按角色覆盖，μ's Final Live 活动休止与蓮ノ空 103 期 3 年生毕业）；日期支持年/月/日三种精度，i18n 补充 timeline 中英文案；构建验证通过（`npm run build`）

### 5.5 企划大事记（嵌入 Home 或独立 `/history`）
- [x] 2010 μ's 结成 → 2025+ 全系列时间线
- [x] 按年份分组，标注里程碑事件（动画化、巨蛋演出、新成员加入）
  - 附注：新建 `src/data/history.js`（45 个里程碑，2010-06-30 μ's 结成 → 2025 Aqours Finale/蓮ノ空世代交替，七类节点：企划始动/CD 出道/动画化/成员变动/重大公演/系列祭典/毕业休止；日期支持年/月/日三种精度，关键日期经 G's 事件年表、官方特设页与 llwiki/fandom 交叉验证——Aqours 结成日 2015-06-30、PDP 公开 2017-03-30、虹咲出道专辑 2018-11-21、Liella! 出道单 2021-04-07、3rd Tour 巨蛋 2023-03、3 期生 11 人体制 2023-06 等）；新增 `History.jsx`（HLTV Events 风格按年份分组时间线：企划标签 + 类型下拉筛选 + 新旧排序切换，里程碑带类型徽章/企划色点，liveId/eventId 内链 Live 与活动详情页）；App 注册 `/history` 路由，Header 导航新增 History 入口，Home Hero 新增大事记按钮；i18n 补充 history 中英文案；构建验证通过（`npm run build`）

---

## Phase 6 · HLTV 风格深化

### 6.1 首页动态流
- [x] 模拟 HLTV 首页布局：左侧赛事结果、中间新闻/动态、右侧排行
- [x] "新闻"条目：数据层面的动态（新单曲发行、Live 举办、成员加入/毕业）
  - 附注：Home 重构为 HLTV 三栏动态流（lg 断点 3/6/3 栅格，移动端单栏堆叠且动态居首）——左栏「最近赛果」展示最新 8 场 Live（企划色点 + 场馆城市 + 日期）；中栏「最新动态」为数据层新闻流（ discs 唱片发行 / lives Live 举办 / 大事记 member・graduation 里程碑三类合并按日期新→旧，条目带类型徽章（发行蓝·Live 绿·成员变动金·毕业红）与企划标签，分别内链唱片/Live 详情与大事记）；右栏 Top 10 选手排行（评分色编码，替代原通栏表格）；Hero 与企划概览卡片保留。`seiyuu.js` 新增 `getNewsFeed` / `getRecentLives` 辅助函数；i18n 补充 home.feed 中英文案（徽章按唱片类型细分）；构建验证通过（`npm run build`）

### 6.2 "转会"系统（HLTV Transfers 映射）
- [x] 成员加入/毕业时间线：虹咲 2-4 期追加、Liella! 2-4 期追加、莲之空世代交替
- [x] 选手页展示"生涯变动"卡片（类似 HLTV 选手转会历史）
  - 附注：新建 `src/data/transfers.js`（四类变动记录 join 加入 / graduation 毕业 / hiatus 活动休止 / recast 声优交棒，按 playerId 关联选手、日期支持三种精度）并覆盖全企划 72 条记录——μ's 结成 2010-06-30 与 Final Live 后活动休止 2016-04-01、Aqours 结成 2015-06-30；虹咲三次追加（栞子 2020-08-03 系列首个成员追加、ミア/嵐珠 2021-08-31 官方公告 12 人体制）及 せつ菜 CV 交棒（楠木ともり 2023-03 末卸任 → 林鼓子自 4th Live 2023-12-23 接任）；Liella! 1 期生 2020-12-14 声优发表、2 期生 2022-05、3 期生 2023-06（无 4 期生，官方 cast 页确认现 11 人体制）；蓮ノ空世代交替（103 期 App 开幕 2023-04、104 期 2024-04-13、103 期 3 年生毕业 2025-03-30 蓮華祭、105 期 2025-04-10 Edel Note 结成、103 期剩余 3 人卒業公演预定 2027-01 日本武道館）；新增 `TransferHistory.jsx` 生涯变动卡片（HLTV 转会列表风格：日期 + 类型色徽章 + 企划色组合链接 + 双语说明，新记录在前）嵌入 PlayerDetail（生涯时间线下方，无记录不渲染）；`seiyuu.js` 新增 `getTransfersByPlayer` 辅助函数；i18n 补充 transfers 中英文案；关键日期经官网公告（虹咲新成员告知 2021.08.31）、官方 cast 页与蓮ノ空生放送年表交叉验证；构建验证通过（`npm run build`）
  - 附注（数据勘误）：修复 players.js 中 Liella! 段声优资料错误——すみれ/恋 CV 互换（真实对应：すみれ=ペイトン尚未、恋=青山なぎさ，恋条目曾误复用きな子 CV 鈴原希実数据，id 同步更正为 `payton-naomi`/`aoyama-nagisa` 并同步 lives.js 与 transfers.js 引用）；按官方 cast 页与事务所资料修正 1-3 期生生日/出身地/事务所共 13 处（岬なこ出身兵庫県・ホーリーピーク、Liyuu ホリプロインターナショナル、鈴原希実宮崎県・Apollo Bay、薮島朱音 2001-07-18 神奈川県・アイムエンタープライズ、大熊和奏 2001-04-13 埼玉県・賢プロダクション、絵森彩山梨県・BOX CORPORATION、結那 2001-09-27 神奈川県・放映新社、坂倉花 2004-04-03 埼玉県・Rush Style、伊達さゆり Apollo Bay、青山なぎさ B 型 155cm クロコダイル等）

### 6.3 选手成就系统（HLTV Trophies 映射）
- [x] MVP 式荣誉：单曲 Center 次数、solo 曲数、巨蛋/甲子园出演
- [x] PlayerDetail 奖杯陈列区
  - 附注：`seiyuu.js` 新增 `getPlayerTrophies` 推导函数，四类荣誉全部从 songs/lives 动态推导不硬编码——center（非 solo 曲 Center 担当，按发行日升序内链歌曲页）、solo（Solo 曲，口径与生涯时间线一致）、dome（巨蛋公演出演：场馆名含 ドーム/Dome 且排除東京ドームシティホール等小型 Hall，按场馆分组计数并内链最近一场 Live）、koshien（ユニット甲子園出演：Live 名含 甲子園/Koshien，内链各场 Live）；count 为 0 的类别不返回；新增 `PlayerTrophies.jsx` 奖杯陈列区卡片（标题 + 荣誉计数、grid 双列奖杯卡片：描边奖杯 SVG + 彩色 ×count + 类型名 + 明细芯片链接，四类各配金/粉/紫/蓝色）嵌入 PlayerDetail（统计网格之后、生涯时间线之前，无成就时组件整体不渲染）；i18n 补充 playerDetail.trophies 中英文案
  - 附注（数据勘误）：为 `unit-koshien-2024` 补录显式 performers（Aqours 9 人 + Saint Snow 2 人 + 虹咲 12 人 + Liella! 11 人 + Sunny Passion 2 人 + 蓮ノ空 103 期 6 人）——原缺省按 groupIds 推导会错误包含 2024-03-09 公演时尚未加入的蓮ノ空 104 期（2024-04-13 加入）/105 期成员；memberStatus 补记楠木ともり缺席（せつ菜役 2023-03 末交棒林鼓子，本公演 A・ZU・NA 由林鼓子出演，沿用 nijigasaki-4th-live 同一口径，由 getLivePerformers 剔除缺席者）；出演阵容经 SPICE/v-storage/CNET 三份公演报告交叉验证；构建验证通过（`npm run build`）

### 6.4 生涯数据曲线
- [x] 选手页增加历年活动量/rating 趋势折线图（Recharts）
  - 附注：`seiyuu.js` 新增 `getCareerActivity` 推导函数——从 lives/songs/events 动态聚合各年份的 Live 出演场次（实际出演口径，剔除缺席）、歌曲参与数（含 solo 单独计数）、重大活动数，首尾年份间空档年补 0 保证折线连续、首尾之外不延伸（已休止企划不拖到当前年份）；年度评分沿用站点综合评分权重按当年增量计算（lives×0.05 + songs×0.02 + solo×0.03 + events×0.005，上限 1.50，粉丝数/唱片数无年度数据不计入）；新增 `CareerChart.jsx` 组件（Recharts LineChart 双轴：左轴 Live 出演绿线/歌曲参与蓝线，右轴年度评分粉色强调线 0–1.50 域，自定义 Tooltip 按站点评分色阶着色并标注 Solo 数，底部附评分口径说明）嵌入 PlayerDetail（奖杯陈列区之后、生涯时间线之前，不足两年数据时组件自身不渲染）；i18n 补充 playerDetail.chart 中英文案；构建验证通过（`npm run build`）

---

## Phase 7 · 工程化与部署

### 7.1 测试（Vitest）
- [x] 安装 Vitest，覆盖纯函数：rating 计算、getDisplayName、i18n 数据映射、seiyuu.js 辅助函数
- [x] 数据完整性校验测试：所有 player 必含 romajiName/characterRomaji、songs 的 groupId 必须存在于 groups
  - 附注：安装 Vitest 4.1.11（`vitest.config.js` node 环境扫描 `src/**/*.test.js`，脚本 `npm test` / `npm run test:watch`），共 4 个测试文件 87 个用例——`src/utils/rating.test.js`（calculateRating 加权口径与 1.50 上限、getRatingColor 四档色阶阈值）；`src/data/seiyuu.test.js`（getDisplayName 中英双语格式与无姓/单名兜底、按 id/角色名检索、歌曲↔唱片↔Live 关联、getLivePerformers 显式名单/全员推导/缺席剔除、resolveSetlistEntry 三种格式兼容、动态流排序、生涯时间线/转会/成就/年度曲线口径）；`src/i18n/i18n.test.js`（t() 嵌套键解析/插值/未知键兜底、zh/en 字典键集合对称、dataTranslations 五组映射函数及全部实际数据值的英文映射覆盖率）；`src/data/dataIntegrity.test.js`（30 项引用完整性：player 必含 romajiName/characterRomaji 与六项 stats、id 与角色名唯一、songs/discs/lives/events/relationships/history/transfers 的 groupId 归属与跨文件引用——setlist songId、disc tracks、singleId、performers、liveIds、playerId、liveId/eventId 全部校验）。配套重构：calculateRating/getRatingColor 原在 8 个页面 + CareerChart 重复定义，提取为 `src/utils/rating.js` 共享模块统一导入（行为不变，build 通过）；测试还抓到并修复一处真实数据缺口——birthplaceEnMap 缺 7 个出身地英文映射（長野/山梨/奈良/千葉/宮崎/石川/中国・上海，Phase 4.4 勘误新引入的值，此前英文模式会漏出日文原值）

### 7.2 性能优化
- [ ] React.lazy 路由级代码分割（11 个页面全部懒加载）
- [ ] 首页首屏体积对比验证

### 7.3 CI
- [ ] GitHub Actions：push/PR 触发 `lint + build + test`

### 7.4 部署上线
- [ ] Vercel（推荐，支持 SPA 路由重写）或 GitHub Pages
- [ ] 自定义 404 → 重定向首页（SPA fallback）
- [ ] README 补充线上地址

---

## Phase 8 · 轻量后端（可选，远期）

> 目标：数据 API 化便于管理与扩展，不做用户系统。

- [ ] 数据层抽象：定义 `dataProvider` 接口（getPlayers/getSongs/...），当前实现为静态 import，保持页面代码不变
- [ ] 数据导出为 JSON：构建脚本将 `src/data/*.js` 导出为静态 JSON，前端 fetch 加载
- [ ] （可选）Hono/Express + SQLite：支持按企划/年份查询、聚合统计下推到后端
- [ ] 验收标准：切换数据源对页面组件零改动

---

## 优先级与依赖关系

```
Phase 4 (数据) ──→ Phase 5 (新页面，依赖新数据)
                ──→ Phase 6.2/6.3 (转会/成就，依赖时间属性)
Phase 7.1 (测试) ──→ 建议尽早做，为 Phase 4 数据改动兜底
Phase 7.2-7.4 (性能/CI/部署) ──→ 可随时穿插，建议 Phase 5 完成后集中做
Phase 8 (后端) ──→ 最后，且可选
```

## 建议节奏

| 批次 | 内容 | 产出 |
|------|------|------|
| 第 1 批 | 4.1 + 4.2 + 7.1（测试先行兜底） | 数据地基 + 安全网 |
| 第 2 批 | 5.1 + 5.2 + 4.3 | 歌曲体系完整闭环 |
| 第 3 批 | 6.1 + 6.2 + 5.4 | HLTV 味道最浓的一批 |
| 第 4 批 | 5.3 + 5.5 + 6.3 + 6.4 | 活动/时间线/成就 |
| 第 5 批 | 7.2 + 7.3 + 7.4 | 上线 |
| 第 6 批 | Phase 8（视维护成本决定） | 数据 API 化 |

---

*最后更新：2026-08-21*
